#include "main.h"

/**
 * _isupper - checks upper case letters
 *
 * @x: variable to be checked
 *
 * Return: 1 for uppercase else 0
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}
